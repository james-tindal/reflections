{
  "name": "audio-recorder",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "build": "tsc",
    "build:infra": "cd infra && pulumi up"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "^4.0.18",
    "playwright": "^1.58.2",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  }
}
